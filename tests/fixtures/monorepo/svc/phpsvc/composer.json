{
    "name": "example/phpsvc",
    "description": "Fixture project for the composer scanner.",
    "type": "project",
    "require": {
        "php": ">=8.1",
        "ext-json": "*",
        "monolog/monolog": "^2.9",
        "example/internal-tools": "dev-main"
    },
    "require-dev": {
        "phpunit/phpunit": "^10.5"
    },
    "config": {
        "sort-packages": true
    }
}
